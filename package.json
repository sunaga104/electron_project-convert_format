{
  "name": "electron_project-convert_format-main",
  "version": "1.0.0",
  "description": "You can change the character format.",
  "main": "index.js",
  "scripts": {
    "start": "electron ./src",
    "build-macOS": "electron-packager ./src FirstApp --platform=darwin --arch=x64 --overwrite",
    "build-windows": "electron-packager ./src FirstApp --platform=win32 --arch=x64 --overwrite"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^20.1.1",
    "electron-builder": "^23.3.3",
    "electron-packager": "^16.0.0"
  }
}
